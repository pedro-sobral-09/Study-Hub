programa {
  funcao inicio() {
  inteiro soma, numero
  caracter resp

  soma = 0
  
   faca{
    escreva("Digite o valor: ")
    leia(numero)
    soma = soma + numero
    escreva("Você quer continuar? [S/N] ")
    leia(resp)
  }enquanto(resp != 'N')
    
  }
}
